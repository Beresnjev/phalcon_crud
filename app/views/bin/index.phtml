<?php use Phalcon\Tag; ?>

<h1 class="welcome-sign">Welcome to bin section!</h1>

<div class="link">
    <a href="/phalcon_crud">Back to main page</a>
</div>

<?php
if ($deleted_count == 0) {
    ?>
    <h2>No anekdots is here....</h2>
    <?php
} else {
    ?>
    <table class="table table-bordered table-hover">
        <thead class="thead-light">
        <tr>
            <th>Anekdot number</th>
            <th>Deleted Author</th>
            <th>Deleted Anekdot</th>
            <th colspan="2" class="table-button"></th>
        </tr>
        </thead>
        <hr>
        <tbody>
        <?php foreach ($anekdots as $anekdot) {
            if ($anekdot->status === "Passive") {
                ?>
                <tr>
                    <td><?php echo $anekdot->number; ?></td>
                    <td><?php echo $anekdot->author; ?></td>
                    <td><?php echo $anekdot->text; ?></td>
                    <td class="table-button">
                        <?php echo $this->tag->form(["action" => "bin/revive", "method" => "post", "class" => "input", "name" => "Revive"]);

                        echo $this->tag->inputSubmit($anekdot->id, "revive");

                        echo Tag::endForm(); ?>
                    </td>
                    <td class="table-button">
                        <?php echo $this->tag->form(["action" => "bin/terminate", "method" => "post", "class" => "input", "name" => "Terminate"]);

                        echo $this->tag->inputSubmit($anekdot->id, "❌");

                        echo Tag::endForm(); ?>
                    </td>
                </tr>
                <?php
            }
        } ?>
        </tbody>
        <tfoot>
        <tr>
            <td colspan="3">Anekdots quantity: <?php echo $deleted_count; ?></td>
            <td colspan="2" class="table-button"></td>
        </tr>
        </tfoot>
    </table>
    <?php
}
?>